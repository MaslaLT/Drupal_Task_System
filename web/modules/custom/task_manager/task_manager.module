<?php

/**
 * @file
 * Provides a task_manager entity type.
 */

use Drupal\Core\Render\Element;

/**
 * Implements hook_theme().
 */
function task_manager_theme() {
  return [
    'task_manager' => [
      'render element' => 'elements',
    ],
  ];
}

/**
 * Prepares variables for task_manager templates.
 *
 * Default template: task-manager.html.twig.
 *
 * @param array $variables
 *   An associative array containing:
 *   - elements: An associative array containing the task_manager information and any
 *     fields attached to the entity.
 *   - attributes: HTML attributes for the containing element.
 */
function template_preprocess_task_manager(array &$variables) {
  foreach (Element::children($variables['elements']) as $key) {
    $variables['content'][$key] = $variables['elements'][$key];
  }
}
