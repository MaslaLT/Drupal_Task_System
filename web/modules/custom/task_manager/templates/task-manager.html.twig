{#
/**
 * @file
 * Default theme implementation to present a task_manager entity.
 *
 * This template is used when viewing a registered task_manager's page,
 * e.g., /admin/content/task-manager)/123. 123 being the task_manager's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_task_manager()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
